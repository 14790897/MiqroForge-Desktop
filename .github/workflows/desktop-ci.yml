name: Desktop CI

on:
  push:
    branches: [develop]
  pull_request_target:
  workflow_dispatch:

jobs:
  # ────────────────────────────────────────────────────────────
  # Job 1: 快速检查 — TypeScript + 单元测试 + mock smoke (无 API key)
  # ────────────────────────────────────────────────────────────
  quick:
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}-quick
      cancel-in-progress: true
    runs-on: ubuntu-latest
    # 最小权限：本 job 会跑 PR 检出代码（npm test / mock smoke），只保留读代码 + 上传失败报告
    # 所需的 actions:write，别继承 pull_request_target 的默认可写 token（zizmor excessive-permissions）。
    permissions:
      contents: read
      actions: write
    defaults:
      run:
        working-directory: apps/desktop

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.ref }}
          # 本 job 会执行 PR 检出的代码；checkout 默认把 job token 写进 .git/config，
          # 那等于把带 actions:write 的凭据放到 PR 代码手里。不持久化，后续步骤也不需要它。
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build renderer
        run: npm run build -- --logLevel warn

      - name: Unit tests (vitest)
        run: npm test

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Mock smoke tests (Playwright)
        # 作为门禁跑：smoke 覆盖渲染层启动路径（协议门 / 登录门 / 主界面）。
        # 此前 continue-on-error 期间，mock 缺 window.miqi.app 让 ChatConsole
        # 挂载即崩，65/75 用例静默失效（#1095 修复时才暴露）。瞬时抖动由
        # playwright.config.ts 的全局 CI retries=2 兜住。
        run: npx playwright test --config=playwright.config.ts --project=smoke --quiet

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/desktop/playwright-report/
          retention-days: 7

  # ────────────────────────────────────────────────────────────
  # Job 2: Electron E2E — 完整桌面应用 + 真实 bridge + LLM
  # ────────────────────────────────────────────────────────────
  electron-e2e:
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}-electron-e2e
      cancel-in-progress: true
    # Fork PRs only get secrets if the author is a collaborator.
    # if: |
    #   github.event_name == 'push' ||
    #   github.event_name == 'workflow_dispatch' ||
    #   github.event.pull_request.head.repo.full_name == github.repository ||
    #   contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.pull_request.author_association)
    runs-on: ubuntu-latest
    # E2E job 检出并执行 PR head 的测试代码，因此只保留最小只读权限 + artifact 写入。
    # release asset 上传移到后面不检出 PR head 的独立 job，避免 PR 控制代码接触 contents:write。
    permissions:
      contents: read
      actions: write
    outputs:
      report-url: ${{ steps.upload-e2e-report.outputs.report-url }}
    defaults:
      run:
        working-directory: apps/desktop

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.ref }}
          # 本 job 在凭据落盘后执行 PR 检出的代码（spec / 构建脚本），checkout 默认会把 job
          # token 写进 .git/config —— 别把它交给 PR 代码（CodeRabbit 安全评审 #1107）。
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          npm ci
          pip install uv
          uv sync
          sudo apt-get update -qq && sudo apt-get install -y -qq bubblewrap

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Build renderer
        run: npm run build -- --logLevel warn

      - name: Generate config.json
        run: |
          mkdir -p ~/.miqi
          cat > ~/.miqi/config.json << 'CONFEOF'
          {
            "providers": {
              "siliconflow": {
                "apiKey": "${{ secrets.DEEPSEEK_API_KEY }}",
                "apiBase": "${{ secrets.DEEPSEEK_API_BASE }}",
                "extraHeaders": null
              }
            },
            "agents": {
              "defaults": {
                "model": "deepseek-v4-pro",
                "maxTokens": 8000,
                "temperature": 0.1,
                "maxToolIterations": 10,
                "runtime": "app_server"
              },
              "commandApproval": { "enabled": false }
            },
            "tools": {
              "web": {
                "search": {
                  "provider": "brave",
                  "apiKey": "${{ secrets.BRAVE_API_KEY }}",
                  "maxResults": 3
                },
                "fetch": { "provider": "builtin" }
              },
              "exec": { "timeout": 30, "envPassthrough": [] }
            }
          }
          CONFEOF

      - name: AI connectivity probe — fail fast if provider unreachable
        # --reporter=list：命令行 reporter 会整组替换 config 里的 html+json（实测：加它之后
        # test-reports/results.json 不再生成），所以探针这一步不产出 JSON 报告 —— 主 E2E 那份
        # 报告不会被它抢先写掉，主步一份都没产出时汇总步能如实报「没找到报告」（#1107）。
        run: |
          xvfb-run --auto-servernum \
            npx playwright test --config=playwright.config.ts --project=electron \
            --grep "AI Connectivity" --reporter=list
        timeout-minutes: 10
        env:
          PLAYWRIGHT_SKIP_WEB_SERVER: '1'

      - name: Run Electron E2E
        # --workers=2 (#1107): ubuntu-latest runners are 4 vCPU, and each worker is a
        # full Electron app plus its Python bridge driving real LLM calls — 4 of them
        # leave the runner no headroom, which surfaces as actionability timeouts at
        # random spec points (first attempt fails, retry passes) and the job still
        # reports success.  Same parallelism as the macOS job below, for the same reason.
        #
        # This is load reduction, not a cure: the macOS job has run 2 workers for a
        # while and still has flaky runs.  The other half of #1107 is the next step,
        # which makes whatever flakiness remains visible instead of silent.
        #
        # Halving parallelism roughly doubles wall clock (the full suite takes 15-20
        # min at 4 workers, 199-233 tests), so the step timeout goes 30 -> 60 min: a
        # 2-worker run lands somewhere in the 30-40 min range, and the estimate has
        # not been confirmed by a real CI run yet.
        run: |
          xvfb-run --auto-servernum \
            npx playwright test --config=playwright.config.ts --project=electron \
            --workers=2
        timeout-minutes: 60
        env:
          PLAYWRIGHT_SKIP_WEB_SERVER: '1'

      - name: Upload E2E JSON report as artifact
        # #1107 / 安全评审：report 先以 artifact 形式暂存，由后面不检出 PR head 的独立 job
        # 上传到 release asset。本 job 不接触 GH_TOKEN/contents:write。
        if: always()
        id: upload-e2e-report
        uses: actions/upload-artifact@v4
        with:
          name: e2e-report-linux
          path: apps/desktop/test-reports/results.json
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Playwright report (E2E)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-e2e
          path: apps/desktop/test-results/
          retention-days: 7

  # ────────────────────────────────────────────────────────────
  # Job 3: macOS Electron E2E — 完整 macOS 桌面应用 + bridge + LLM
  # ────────────────────────────────────────────────────────────
  macos-e2e:
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}-macos-e2e
      cancel-in-progress: true
    runs-on: macos-latest
    # 同 Linux：只保留最小只读权限 + artifact 写入，release asset 上传交给独立 job。
    permissions:
      contents: read
      actions: write
    outputs:
      report-url: ${{ steps.upload-e2e-report.outputs.report-url }}
    defaults:
      run:
        working-directory: apps/desktop

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.ref }}
          # 本 job 在凭据落盘后执行 PR 检出的代码（spec / 构建脚本），checkout 默认会把 job
          # token 写进 .git/config —— 别把它交给 PR 代码（CodeRabbit 安全评审 #1107）。
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          npm ci
          pip install uv
          uv sync

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Build renderer
        run: npm run build -- --logLevel warn

      - name: Generate config.json
        run: |
          mkdir -p ~/.miqi
          cat > ~/.miqi/config.json << 'CONFEOF'
          {
            "providers": {
              "siliconflow": {
                "apiKey": "${{ secrets.DEEPSEEK_API_KEY }}",
                "apiBase": "${{ secrets.DEEPSEEK_API_BASE }}",
                "extraHeaders": null
              }
            },
            "agents": {
              "defaults": {
                "model": "deepseek-v4-pro",
                "maxTokens": 8000,
                "temperature": 0.1,
                "maxToolIterations": 10,
                "runtime": "app_server"
              },
              "commandApproval": { "enabled": false }
            },
            "tools": {
              "web": {
                "search": {
                  "provider": "brave",
                  "apiKey": "${{ secrets.BRAVE_API_KEY }}",
                  "maxResults": 3
                },
                "fetch": { "provider": "builtin" }
              },
              "exec": { "timeout": 30, "envPassthrough": [] }
            }
          }
          CONFEOF

      - name: AI connectivity probe (macOS) — fail fast if provider unreachable
        # 同 Linux 侧：--reporter=list 让探针不产出 JSON 报告，不占用主 E2E 那份（#1107）。
        run: |
          npx playwright test --config=playwright.config.ts --project=electron \
            --grep "AI Connectivity" --reporter=list
        timeout-minutes: 10
        env:
          PLAYWRIGHT_SKIP_WEB_SERVER: '1'

      - name: Run Electron E2E (macOS)
        # --workers=2: macos-latest runners (3 vCPU) can't sustain 4 parallel
        # Electron instances + LLM calls — overloaded runners cause intermittent
        # failures at random spec points (actionability misjudgements, slow
        # first sessions.list, sandbox timeouts).  Halving parallelism made the
        # suite stable without changing test semantics.
        #
        # --grep-invert also drops describe blocks that burn the most wall-clock
        # on macOS so the job stays under ~15 min:
        #   - sandbox-only suites (Sandbox Toggle / Sandbox toggle ready /
        #     Workspace Switch E2E (Sandbox ON)): no bubblewrap on macOS, so they
        #     wait waitForSandboxReady(300s) + LLM retries before failing.
        #   - heaviest LLM suites (Native Electron E2E / Feedback Page E2E /
        #     Execution Policy E2E): 34 long streaming tests that double the run.
        # Both are fully covered by the Linux electron-e2e job, which runs the
        # whole suite.
        #
        # --workers=2 cut the flakiness down but did not remove it — a 6-run sample
        # still had 0-2 flaky specs per run, with repeated hits on the same test
        # (task-assets.spec.ts:143, which looks deterministic rather than
        # load-induced).  That is why the summarize step below exists instead of
        # another round of parallelism tuning.
        run: |
          npx playwright test --config=playwright.config.ts --project=electron \
            --grep-invert "Sandbox Exec|Sandbox Toggle|Sandbox toggle ready|Workspace Switch E2E \(Sandbox ON\)|session-key-mapping|PPTX Generator|Native Electron E2E|Feedback Page E2E|Execution Policy E2E" \
            --workers=2
        timeout-minutes: 45
        env:
          PLAYWRIGHT_SKIP_WEB_SERVER: '1'

      - name: Upload E2E JSON report as artifact (macOS)
        if: always()
        id: upload-e2e-report
        uses: actions/upload-artifact@v4
        with:
          name: e2e-report-macos
          path: apps/desktop/test-reports/results.json
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload Playwright report (macOS E2E)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-macos-e2e
          path: apps/desktop/test-results/
          retention-days: 7

  # ────────────────────────────────────────────────────────────
  # Job 4: 上传 E2E JSON 报告到 release asset —— 不检出 PR head，不执行 PR 控制代码。
  # E2E job 先把报告以 artifact 形式留档，本 job 下载后用 gh CLI 上传到固定预发布。
  # 这里持 contents:write，但整个 job 不接触 PR 检出代码，GH_TOKEN 只交给 trusted gh CLI。
  # ────────────────────────────────────────────────────────────
  upload-e2e-reports:
    needs: [electron-e2e, macos-e2e]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    outputs:
      linux-report-url: ${{ steps.upload.outputs.linux-report-url }}
      macos-report-url: ${{ steps.upload.outputs.macos-report-url }}

    steps:
      - name: Download Linux E2E report
        uses: actions/download-artifact@v4
        with:
          name: e2e-report-linux
          path: reports/linux/
        continue-on-error: true

      - name: Download macOS E2E report
        uses: actions/download-artifact@v4
        with:
          name: e2e-report-macos
          path: reports/macos/
        continue-on-error: true

      - name: Upload reports to release asset
        id: upload
        run: |
          set -euo pipefail
          tag="_gh-artifacts"
          if ! gh release view "$tag" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            # 不同 PR / 不同平台的运行可能同时创建同一个 release：创建失败说明别家刚建好，
            # 重读一次复用即可，别让 set -e 把整步判失败（CodeRabbit 评审 #1107）。
            gh release create "$tag" --prerelease --title "$tag" \
              --body "CI 产物（E2E 报告等）—— 不要删除。见 .github/workflows/desktop-ci.yml" \
              --repo "${{ github.repository }}" 2>/dev/null || \
              gh release view "$tag" --repo "${{ github.repository }}" >/dev/null
          fi

          linux_url=""
          if [ -f reports/linux/results.json ]; then
            linux_name="e2e-results-linux-${{ github.run_id }}-${{ github.run_attempt }}.json"
            cp reports/linux/results.json "$linux_name"
            gh release upload "$tag" "$linux_name" --clobber --repo "${{ github.repository }}"
            linux_url="https://github.com/${{ github.repository }}/releases/download/$tag/$linux_name"
            rm -f "$linux_name"
          fi

          macos_url=""
          if [ -f reports/macos/results.json ]; then
            macos_name="e2e-results-macos-${{ github.run_id }}-${{ github.run_attempt }}.json"
            cp reports/macos/results.json "$macos_name"
            gh release upload "$tag" "$macos_name" --clobber --repo "${{ github.repository }}"
            macos_url="https://github.com/${{ github.repository }}/releases/download/$tag/$macos_name"
            rm -f "$macos_name"
          fi

          # 保留最近 100 个资产（约 50 次双平台运行），避免触及 release 1000 资产上限。
          gh api "repos/${{ github.repository }}/releases/tags/$tag" \
            --jq '.assets | sort_by(.created_at) | reverse | .[100:] | .[].id' | \
            xargs -r -I {} gh api -X DELETE "repos/${{ github.repository }}/releases/assets/{}" --silent

          echo "linux-report-url=$linux_url" >> "$GITHUB_OUTPUT"
          echo "macos-report-url=$macos_url" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ────────────────────────────────────────────────────────────
  # Job 5: flaky 汇总 —— 无 provider secrets，只下载并解析主 E2E 上传的 JSON 报告（#1107）。
  # 这个 job 没有 provider 凭据，因此即便解析脚本来自 PR 检出，也没有 API key 可被窃取；
  # 但脚本本身确实在 PR 检出的代码上执行（与仓库现有 mock smoke/e2e spec 的信任模型一致）。
  # ────────────────────────────────────────────────────────────
  summarize-flaky:
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}-summarize-flaky
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: [upload-e2e-reports]
    if: always()
    defaults:
      run:
        working-directory: apps/desktop

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request_target' && github.event.pull_request.head.sha || github.ref }}
          # 这里同样执行 PR 检出的脚本；token 在本 job 只是只读，但没必要留在工作区里。
          persist-credentials: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json

      - name: Summarize flaky E2E tests (Linux)
        if: needs.upload-e2e-reports.outputs.linux-report-url != ''
        run: |
          mkdir -p test-reports
          curl -fsSL "${{ needs.upload-e2e-reports.outputs.linux-report-url }}" -o test-reports/results-linux.json
          node scripts/e2e-flaky-report.mjs test-reports/results-linux.json

      - name: Summarize flaky E2E tests (macOS)
        if: needs.upload-e2e-reports.outputs.macos-report-url != ''
        run: |
          mkdir -p test-reports
          curl -fsSL "${{ needs.upload-e2e-reports.outputs.macos-report-url }}" -o test-reports/results-macos.json
          node scripts/e2e-flaky-report.mjs test-reports/results-macos.json

      - name: E2E 报告缺失说明
        if: needs.upload-e2e-reports.outputs.linux-report-url == '' && needs.upload-e2e-reports.outputs.macos-report-url == ''
        run: echo "两个 E2E job 都没有产出 JSON 报告（主步被杀/OOM），见上面的说明"

  # ────────────────────────────────────────────────────────────
  # macOS 打包（dmg/zip）不在这里跑：每个 PR 都起 macos runner 成本高，
  # 产物也没有下游消费方。发版时由 release.yml 的 build-mac 构建并上传 release。
  # ────────────────────────────────────────────────────────────
