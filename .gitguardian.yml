# GitGuardian 扫描忽略清单
#
# 说明：以下条目为「面向所有客户端分发的共享凭据」，由平台侧统一
# 管理与轮换，并非个人密钥——写在仓库里是产品要求的开箱即用默认值
#（与 schema.py FeedbackConfig 中已有硬编码凭据同一性质）。
secret:
  ignored-matches:
    # 平台托管 slurm MCP 网关的共享 Bearer token（2026-09-05 产品确认
    # 内置默认服务器用；轮换由平台侧统一执行，见 miqi/config/schema.py
    # DEFAULT_MCP_SERVERS 注释）
    - name: miqroforge-slurm-gateway-shared-token
      match: "Y4rmjqg5NeGqglzyGNK_HJvmATyCzKXv4p2ZBy-E08I"
